# Local Profiles v2

Fixes the settings-screen crash from v1 by using React Native's own
View/Text/Pressable/Alert components instead of Kettu UI components.

Upload `index.js` and `manifest.json` over the existing files in your repo,
then reload/update the plugin in Kettu.
